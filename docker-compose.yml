services:
  api:
    build:
      context: ./asisya_api/asisya_api
      dockerfile: Dockerfile
    ports:
      - "5000:8080"

  frontend:
    build:
      context: ./asisya_Frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - api